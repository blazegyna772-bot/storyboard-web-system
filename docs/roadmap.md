# 路线图

## P0 当前阶段

目标：需求方案、设计方案、前端原型、可扩展骨架。

已完成：

- 四个一级页面
- 剧本校验原型
- 资产生图页原型
- 上下文包页原型
- 分镜工作台原型
- 文档追踪体系

已完成：

- Pipeline 代码骨架
- 流程配置页
- Rule Pack / Profile / Output Adapter 的默认配置
- 资产、辅助信息、分镜页面的操作型布局初版
- 基础工作台壳子：顶部窄栏、左侧可收起导航、右侧覆盖式工具抽屉

P0 最后收口：

- Artifact Store：原型完成，缺完整内容快照。
- 锁定/重跑模型：已有锁定/解锁和重跑请求，缺真实局部 executor。
- 任务状态模型：原型完成，缺异步任务队列。
- 输出真源结构稳定：已迁出 export adapter，仍需多平台 adapter。
- 保存 artifact / latestRun / lock 状态：已接入本地项目。

已完成但仍需增强：

- `PipelineRun` / `StageResult` / execution logs
- 本地项目、多项目、保存版本摘要
- 底部执行反馈和开发排查面板

## P1 AI 执行器阶段

目标：接入 LLM，先解决源头和上下文。

范围：

- LLM 剧本清洗
- LLM 剧本审校
- LLM 上下文包生成
- JSON schema 校验
- LLM 调用日志和阶段产物追踪

当前进展：

- 已完成 Executor 接口与 DeepSeek/OpenAI-compatible 本地代理。
- 已完成 01、03、04 Prompt 模板和输出契约。
- 已完成清洗稿、集级辅助、场级上下文、资产描述 Schema 校验。
- 已把 01、03、04、05 接入异步 `PipelineRun`、日志台和 Artifact Store。
- 已移除前端 Mock 选项；真实调用失败会进入日志。
- 尚未完成异步任务队列、后端持久化和 06 之后的真实模型阶段。

## P2 资产视觉阶段

目标：资产图生成和锁定。

范围：

- 生图接口：已接本地 `/api/image/generate` 代理。
- 候选图：已接 `AssetImageCandidate`。
- 锁定版本：已接候选图锁定并同步资产 LockRecord。
- 资产图进入分镜上下文：锁定状态已可被后续阶段引用，真实 06/08 仍待接入。

## P3 分镜生产阶段

目标：真正的视听语言分镜。

范围：

- 戏眼提取
- 情绪段切分
- 镜头覆盖设计
- 分镜生成
- 分镜复核

## P4 生产系统阶段

目标：可用于真实项目。

范围：

- 后端
- 数据库
- 任务队列
- 项目权限
- 版本历史
- 批量导出
